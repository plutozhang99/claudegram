## Project: claudegram v1 — P1 (PWA skeleton + WebSocket live push)

## Spec Files
- docs/request_v1.md
- docs/archive/PROGRESS-P0-2026-04-18.md (P0 reference, shipped)

## Current Phase: P1 COMPLETE — all phases shipped + retro review closed + live smoke PASS + browser-verified. 192 tests pass; tsc clean; ready to tag.

## Interruption Reason


## Rate Limit State


## Review Roster (P1 roster; A11y newly activated — PWA UI arrives in this phase)
固定:
- Slot 1 Code Review: typescript-reviewer agent (no typescript-review skill installed)
- Slot 2 Security Review: security-review skill
- Slot 3 Functional Coverage: functional-coverage skill (always)
条件性 (仅列出已激活的):
- Slot 4 DB Review: database-reviewer agent
- Slot 5 A11y Review: a11y-architect agent (PWA shell only — P1.5/1.6)
- Slot 6 Type Review: type-design-analyzer agent
- Slot 7 Error Review: silent-failure-hunter agent

Not activated for P1:
- Slot 8 Perf: no high-perf requirement (single-user PWA)
- Slot 9 Clinical: N/A

## Active Task
None — P1 shipped. Next user action: push + tag if desired; start P2 planning (fakechat reverse WS + user replies).

## Completed Tasks
- [x] **P1.1** schema evolution + repo extensions — commit e160b46 — 2 rounds — code ✅ sec ✅ func ✅ db ✅ type ✅ err ✅
- [x] **P1.2 + P1.3** API routes + WebSocket broadcast hub — commit 1f81c9e — 2 rounds — code ✅ sec ✅ func ✅ type ✅ err ✅
- [x] **P1.4** static file serving + traversal guard + nosniff + extension allowlist — commit dadf32d — 2 rounds — sec ✅ func ✅
- [x] **P1.5** PWA shell (index.html, style.css, manifest, sw.js, 192/512 icons, module stubs) + integration tests — commit 7b64393 — 2 rounds — a11y ✅ func ✅ (via integration-static.test.ts)
- [x] **P1.6** PWA JS modules (ws, store, render, notify, index) + parse tests — commit dacbec1 — 1 round — func ✅ (parse-check + import graph)
- [x] **P1.7** integration E2E + API integration + README update — 1 round — func ✅ (13 real-HTTP/WS tests)

## Pending Tasks (prioritized)

_None — all P1 tasks shipped. Final state below for historical reference._

### P1.2 — API routes  ✅ shipped in commit 1f81c9e
- [x] **1.2a** `src/routes/api/sessions.ts` — `GET /api/sessions` → `{ ok, sessions: SessionListItem[] }`; 405 non-GET; 500 on repo throw w/ contextual logger
- [x] **1.2b** `src/routes/api/messages.ts` — `GET /api/messages?session_id=X&before=MID&limit=N` — Zod `z.coerce.number()` validation, 400 on invalid, empty + has_more:false on unknown session
- [x] **1.2c** `src/routes/api/me.ts` — reads `Cf-Access-Authenticated-User-Email` only when `TRUST_CF_ACCESS=true`; fallback `local@dev`
- [x] **1.2d** dispatcher wiring + 404 on unknown `/api/*`

### P1.3 — WebSocket broadcast hub  ✅ shipped in commit 1f81c9e
- [x] **1.3a** `src/ws/hub.ts` — `Hub` interface + `InMemoryHub` with typed `BroadcastPayload` discriminated union; per-socket catch isolation; JSON.stringify once per broadcast
- [x] **1.3b** `src/server.ts` — Bun.serve `fetch`+`websocket` handler; upgrade at `/user-socket` only
- [x] **1.3c** `src/routes/ingest.ts` — on 200: broadcasts `{type:'message', session_id, message{…,ingested_at}}` then `{type:'session_update', session}`; wrapped in try/catch so broadcast failure never fails ingest
- [x] **1.3d** WS lifecycle: open/close logged, size tracked, client messages ignored (reserved for P2)

### P1.4 — Static file serving  ✅ shipped in commit dadf32d
- [x] **1.4a** `src/routes/static.ts` — `serveStaticFile` with `path.resolve` + prefix-check traversal guard; strict extension allowlist (html/js/mjs/css/json/webmanifest/png/svg/ico/txt); `X-Content-Type-Options: nosniff`; 404 on miss/extensionless/unmapped
- [x] **1.4b** dispatcher: `/` → `handleRoot` (405 on non-GET), `/web/*` → `handleWebAsset` (405 on non-GET)

### P1.5 — PWA shell (HTML/CSS/manifest/sw)  ✅ shipped in commit 7b64393
- [x] **1.5a** `web/index.html` — semantic HTML5, `<h1>` landmark, `role="listbox"` sessions, `aria-live="polite"` messages, skip-link, SW registration guard
- [x] **1.5b** `web/style.css` — dark slate palette, CSS Grid (260px + 1fr), 44px touch targets, `@media (max-width: 640px)` responsive sidebar, `prefers-reduced-motion` guard
- [x] **1.5c** `web/manifest.webmanifest` — standalone display, maskable 192/512 icons, theme_color `#1e293b`
- [x] **1.5d** `web/sw.js` — versioned `v1-p15` cache, app-shell preload, network-only pass-through for `/api/*` and `/user-socket`, skipWaiting + clientsClaim
- [x] **1.5e** `web/icons/icon-{192,512}.png` — solid `#1e293b` PNGs generated by `scripts/generate-icons.mjs` (zero-dep zlib)
- [x] **a11y Round 2 fixes** — WCAG 2.2 AA: 44px toggle, `<h1>` landmark, `role="listbox"`, removed duplicate `aria-label`, brightened pill text for contrast, reduced-motion guard

### P1.6 — PWA JS modules  ✅ shipped in commit dacbec1
- [x] **1.6a** `web/js/ws.js` — WebSocket client, exp backoff 250ms→8s cap, typed emitter, updates `#conn-pill` via `data-state`
- [x] **1.6b** `web/js/store.js` — Map-backed in-memory state, `applyLiveMessage` with unread_count bump, `hydrateMessages` idempotent
- [x] **1.6c** `web/js/render.js` — listbox sessions with `role="option"` + `tabindex="0"`, message render w/ auto-scroll, compose enable/disable
- [x] **1.6d** `web/js/notify.js` — P3 stub (permission check only, no fires)
- [x] **1.6e** `web/js/index.js` — boot `/api/me` → `/api/sessions` → select-first → hydrate → wire WS; sidebar toggle
- [x] **parse gate** — `src/integration-pwa-js.test.ts` Bun.Transpiler scan + import graph

### P1.7 — Integration + docs  ✅ shipped in commit 0a74d08
- [x] **1.7a** `src/integration.test.ts` — 4 real-WS E2E tests: message+session_update on 200, no events on 400, 2-client fan-out, `ingested_at` timestamp window
- [x] **1.7b/c** `src/integration-api.test.ts` — 9 real-HTTP tests: /api/sessions empty + unread semantics (user=0, assistant=1), multi-session ORDER BY DESC, /api/messages pagination + before_id, /api/me with `trustCfAccess=false`
- [x] **1.7d** `README.md` updated — P1 Features, Running the PWA, WebSocket contract + event shapes, `TRUST_CF_ACCESS` env, Test commands, `generate-icons` script

### Out of scope for P1 (deferred — queued for future phases)
- **P2** — fakechat reverse WebSocket + user replies (unlocks PWA → Claude Code reply flow)
- **P3** — browser Notification API + unread badges + per-session mute
- **P4** — cloudflared tunnel + CF Access SSO + launchd/CLI (production deployment)
- **P5** — Web Push (VAPID) for fully-killed PWA delivery
- **P6** — permission prompts (subsume v0's approve/deny use case)

### Retro-fill summary (post-archive review pass)

After initial archive, a retro review pass covered the 4 review slots that had been skipped during P1.4–P1.7 under context pressure (code/type/err on P1.4, code/sec on P1.5 + P1.6, test-quality/README on P1.7). Findings: 1 HIGH (P1.5 SW scope), 3 MINORs applied, rest accepted to P2. Fixes landed in commit `8a3edcb`.

### Live smoke test summary (2026-04-19)

End-to-end validation against a running daemon — the last gap beyond automated tests. Verified live:
- `/health`, `/api/sessions` (empty + populated), `/api/messages`, `/api/me` (fallback `local@dev`) all respond correctly.
- `POST /ingest` writes to SQLite, returns 200 with `message_id`.
- Real WebSocket client at `/user-socket` receives `{type:'message'}` then `{type:'session_update'}` in order on ingest, `ingested_at` present in broadcast payload.
- Unread count correctly tracks direction='assistant' only; user messages don't bump it.
- All PWA static assets (`/`, `/sw.js`, `/web/style.css`, `/web/manifest.webmanifest`, PNG icons) return 200 with correct MIME + `X-Content-Type-Options: nosniff`.
- Security: `/web/../etc/passwd` → 404 (traversal guard works); `/web/stray.env` with file-on-disk → 404 (allowlist rejects unmapped extension).
- Validation: invalid `/ingest` payloads return 400 with `issues`, no broadcast; method-mismatch returns 405.
- SIGTERM triggers `shutdown_start → ws_close → shutdown_complete` cleanly.

**User-confirmed** frontend renders correctly in a real browser. P1 is production-ready for localhost deployment.

### Non-blocking tech debt carried from P1
- **schema_version table** — additive-only `PRAGMA table_info` detection used this phase; stand up a version table when P2 adds destructive migrations
- **WebSocket auth** — pre-CF-Access any localhost process can read broadcasts; documented P4 prerequisite
- **WS connection cap** — `InMemoryHub` unbounded; add `MAX_WS_CONNECTIONS` guard before P4 public exposure
- **`ingested_at` drift** — broadcast uses `Date.now()` at emit time (1–2ms off DB value); cheap to replace with post-insert `findOne` if drift matters
- **Cache-Control headers** — static responses carry none; tune per-asset TTL at P4 production cut-over

## Review Log
| Task | Code | Sec | Func | DB | A11y | Type | Err | Rounds | Result |
|------|------|-----|------|----|------|------|-----|--------|--------|
| P1.1 R1 | PASS w/ MINOR (H1 stubs, M composite cursor, M transaction, M NaN) | PASS w/ MINOR (M cursor, L NaN) | PASS | PASS w/ MINOR (M transaction, M composite cursor) | N/A | PASS w/ MINOR | FAIL w/ MINOR (M transaction, M NaN) | 1 | ⚠️→R2 |
| P1.1 R2 | PASS (all fixes verified) | N/A | PASS | PASS | N/A | N/A | N/A | 2 | ✅ COMPLETE |
| P1.2+P1.3 R1 | PASS w/ MINOR (M types, M error-handling) | PASS w/ MINOR (M header trust, M WS auth P4) | PASS w/ MINOR (R15 untested) | N/A | N/A | PASS w/ MINOR (M broadcast type, M response types) | PASS w/ MINOR (M error-handling, L ingested_at drift) | 1 | ⚠️→R2 |
| P1.2+P1.3 R2 | PASS (all fixes verified) | N/A | PASS | N/A | N/A | N/A | N/A | 2 | ✅ COMPLETE |
| P1.4 R1 | PASS | PASS w/ MINOR (M nosniff, M allowlist, L traversal comment) | PASS | N/A | N/A | PASS | PASS | 1 | ⚠️→R2 |
| P1.4 R2 | PASS (nosniff header + allowlist enforced + comment added; +2 tests) | PASS | PASS | N/A | N/A | N/A | N/A | 2 | ✅ COMPLETE |
| P1.5 R1 | N/A (content only) | N/A | PASS (7 integration tests for all shell assets) | N/A | PASS w/ MINOR (M touch target, M duplicate label, M contrast, L session keyboard, L reduced-motion, L h1) | N/A | N/A | 1 | ⚠️→R2 |
| P1.5 R2 | PASS (all a11y fixes applied: 44px toggle, h1 landmark, role=listbox, removed duplicate aria-label, brightened pill text, prefers-reduced-motion guard) | N/A | PASS | N/A | PASS | N/A | N/A | 2 | ✅ COMPLETE |
| P1.6 R1 | N/A (vanilla JS, integration-only per Key Decisions) | N/A | PASS (parse + import graph + spec-contract pseudocode) | N/A | Inherited from P1.5 | N/A | N/A | 1 | ✅ COMPLETE |
| P1.7 R1 | N/A (tests + docs only) | N/A | PASS (13 real-HTTP/WS integration tests: sessions, messages, /api/me, WS broadcast fan-out, 400-no-broadcast, ingested_at window) | N/A | N/A | N/A | N/A | 1 | ✅ COMPLETE |
| **RETRO** P1.4 | PASS-WITH-MINOR (logger.debug on unmapped ext + nosniff-on-root test added) | already PASS in P1.4 R2 | PASS | N/A | N/A | PASS | PASS | retro | ✅ retro-closed |
| **RETRO** P1.5 | PASS-WITH-MINOR | **FAIL→PASS** (HIGH: SW registered at `/web/sw.js` had default scope `/web/` → could not cache `/`. Fix: serve sw.js at `/sw.js` via dispatcher + bump cache version to `v1-p17`) | PASS (added `/sw.js` integration test) | N/A | (already PASS) | N/A | N/A | retro | ✅ retro-closed |
| **RETRO** P1.6 | PASS-WITH-MINOR (console.warn + String(e) instead of raw e in handler catch) | PASS-WITH-MINOR (scroll-jank on live msg + console logs — both accepted to P2 in addenda) | (already PASS) | N/A | N/A | N/A | N/A | retro | ✅ retro-closed |
| **RETRO** P1.7 | PASS-WITH-MINOR (test hygiene: 50ms sleeps in WS finally blocks — acceptable; `as unknown as Config` cast — acceptable; false positive on `docs/request_v1.md` link — file exists) | N/A | (already PASS) | N/A | N/A | N/A | N/A | retro | ✅ retro-closed |
| **LIVE SMOKE** 2026-04-19 | PASS (daemon started on :8788, curl health/api/ingest/static all green; WS client received both broadcast events in correct order; traversal + allowlist blocked; SIGTERM graceful shutdown logged) | PASS (path traversal + `/web/stray.env` both 404, nosniff on all static responses) | PASS | N/A | PASS (user-confirmed real browser render OK) | N/A | N/A | smoke | ✅ SHIPPABLE |

## Key Decisions & Accepted Risks

### Architecture (P1)
- 2026-04-18 **unread_count computation**: `count(direction='assistant' AND ts > sessions.last_read_at)`. P1 has no `mark_read` yet (P2 adds it), so `last_read_at` stays 0 by default and unread grows monotonically. Accepted: UX shows "N messages since boot" until P2 lands.
- 2026-04-18 **Schema evolution without version table**: P0 deferred `schema_version` to P1. For P1's additive-only change, `PRAGMA table_info` detection used. Tech debt, introduce schema_version when P2 adds destructive migrations.
- 2026-04-18 **Composite `(ts, id)` cursor** (added R2): pagination filters on `(ts < cursor.ts OR (ts = cursor.ts AND id < cursor.id))` with `ORDER BY ts DESC, id DESC`. Prevents skip/duplicate on duplicate timestamps.
- 2026-04-18 **NaN guard on limit** (added R2): `Number.isFinite(candidate)` check before clamp in both `findBySession` and `findBySessionPage`.
- 2026-04-18 **Migration transaction** (added R2): ALTER statements now wrapped in BEGIN/COMMIT with ROLLBACK on throw.
- 2026-04-18 **API cursor format**: `before=MID` (message_id) per spec §12.6.
- 2026-04-18 **`has_more` via limit+1 fetch**: avoids second COUNT query.
- 2026-04-18 **WebSocket hub scope**: single global in-memory `Set<ServerWebSocket>`. P1 broadcasts to all PWA clients; P2 adds per-session filtering when fakechat reverse-WS lands.
- 2026-04-18 **`/api/me` header trust** (updated in P1.2+P1.3 R1): reads `Cf-Access-Authenticated-User-Email` ONLY when `TRUST_CF_ACCESS=true` env flag is set. Default false → always returns `local@dev`. Prevents pre-P4 header spoofing when the service is accidentally exposed beyond localhost.
- 2026-04-18 **Static file root + hardening**: `current/claudegram/web/`. Traversal guard via `path.resolve` + root-prefix check + early `includes('..')` sieve. Strict extension allowlist (html/js/mjs/css/json/webmanifest/png/svg/ico/txt) — unmapped extensions return 404, NOT `application/octet-stream` (prevents `.env`/`.pem` leakage). All static responses carry `X-Content-Type-Options: nosniff`.
- 2026-04-18 **Service worker scope** (fixed in retro): `sw.js` is served at **both** `/sw.js` (canonical, default scope `/` → can cache root navigation) and `/web/sw.js` (legacy path kept for compat). PWA registers `/sw.js`. Cache version `v1-p17`.
- 2026-04-18 **No framework, no build step**: vanilla ESM per spec §12.3.

### Process
- 2026-04-18 Teams: single agents for P1 (review surface fits without coordination overhead).
- 2026-04-18 TDD applies to: 1.1, 1.2a-c, 1.3a + 1.3c, 1.4a, 1.7a-c.
- 2026-04-18 Coverage targets: repo 90%, API routes 85%, WS hub 85%, static resolver 80%, frontend JS covered by integration only.
- 2026-04-18 Orchestrator skipped mandatory architect (opus) pass per user directive "skip model check follow the plan and keep moving".

## Next Agent Prompt

N/A — P1 is archived. All sub-tasks shipped, retro reviews closed, live smoke + browser verification PASS.

To resume into **P2** (fakechat reverse WebSocket + user replies, per `docs/request_v1.md §7`), a new session should create a fresh `docs/progress/PROGRESS.md` scoped to P2 and run `/start-project docs/progress/PROGRESS.md` to dispatch.
